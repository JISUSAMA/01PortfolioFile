using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SVNTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //여기서 수정하면 같이 되는건가 ??????
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
